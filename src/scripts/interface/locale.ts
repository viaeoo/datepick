export interface Locale {
  days: Array<string>;
  daysShort: Array<string>;
  months: Array<string>;
  monthsShort: Array<string>;
  today: string;
  clear: string;
  format: string;
  titleFormat: string;
  dowFormat: string;
}
